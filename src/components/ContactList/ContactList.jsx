


const ContactList = () => (
  <ul>
    <p>Contacts</p>
    {/* {contacts.map(({ id, name, number }) => (
      <li key={id}>
        <p>{name}:</p>
        <p>{number}</p>
        <button type="button">
        </button>
      </li>
    ))} */}
  </ul>
);

export default ContactList;


